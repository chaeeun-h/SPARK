# SPARK Emotion Classification Fine-tuning

This directory contains scripts to fine-tune multiple transformer models (RoBERTa, BERT, DeBERTa, KoBERT, KR-ELECTRA) for emotion classification on the SPARK dataset.

## Dataset

The SPARK dataset contains Korean social media posts with English translations and emotion labels. We've split it into:
- Train: 1,394 samples (80%)
- Validation: 174 samples (10%)
- Test: 173 samples (10%)

Available in both English (`sentence_en`) and Korean (`sentence_ko`) versions.

## Project Structure

```
SPARK/
├── python/                       # Python scripts
│   ├── spark_data_split.py       # Dataset splitting
│   ├── bert_spark_finetuning.py
│   ├── roberta_spark_finetuning.py
│   ├── deberta_spark_finetuning.py
│   ├── kobert_spark_finetuning.py
│   └── krelectra_spark_finetuning.py
├── data/                         # Dataset files
│   ├── SPARK_master_dataset.csv  # Full master dataset (1,736 sentences)
│   └── spark_*_{train,val,test}.json  # Generated by spark_data_split.py
├── logs/                         # Training logs
│   ├── bert_spark_english/
│   ├── roberta_spark_english/
│   ├── deberta_spark_english/
│   ├── kobert_spark_korean/
│   └── krelectra_spark_korean/
├── SPARK_emotion_analysis.ipynb  # EDA and visualization notebook
└── SPARK_FINE_TUNING_README.md   # This file
```

## Scripts Overview

### 1. Data Preparation
- `python/spark_data_split.py`: Splits the dataset and creates JSON files for training

### 2. BERT Fine-tuning (English)
- `python/bert_spark_finetuning.py`: Fine-tunes BERT-base for emotion classification
- `python/bert_spark_testing.py`: Evaluates the fine-tuned BERT model

### 3. RoBERTa Fine-tuning (English)
- `python/roberta_spark_finetuning.py`: Fine-tunes RoBERTa-base for emotion classification
- `python/roberta_spark_testing.py`: Evaluates the fine-tuned RoBERTa model

### 4. DeBERTa Fine-tuning (English)
- `python/deberta_spark_finetuning.py`: Fine-tunes DeBERTa-base for emotion classification
- `python/deberta_spark_testing.py`: Evaluates the fine-tuned DeBERTa model

### 4. KoBERT Fine-tuning (Korean)
- `python/kobert_spark_finetuning.py`: Fine-tunes KoBERT for Korean emotion classification
- `python/kobert_spark_testing.py`: Evaluates the fine-tuned KoBERT model
  
- ### 4. KoBERT Fine-tuning (Korean)
- `python/krelectra_spark_finetuning.py`: Fine-tunes KR-ELECTRA for Korean emotion classification
- `python/krelectra_spark_testing.py`: Evaluates the fine-tuned KR-ELECTRA model
  
## Usage

### Step 1: Prepare Data
```bash
python3 python/spark_data_split.py
```

This generates six JSON files in `data/`:
- `spark_english_train.json`, `spark_english_val.json`, `spark_english_test.json`
- `spark_korean_train.json`, `spark_korean_val.json`, `spark_korean_test.json`

### Step 2: Fine-tune Models

#### English Models:
```bash
python3 python/bert_spark_finetuning.py
python3 python/roberta_spark_finetuning.py
python3 python/deberta_spark_finetuning.py
```

#### Korean Models:
```bash
python3 python/kobert_spark_finetuning.py
python3 python/krelectra_spark_finetuning.py
```

### Step 3: Evaluate Models
```bash
# RoBERTa evaluation
python3 python/roberta_spark_testing.py

# BERT evaluation
python3 python/bert_spark_testing.py

# DeBERTa evaluation
python3 python/deberta_spark_testing.py

# Llama evaluation
python3 python/llama_spark_testing.py

# KoBERT evaluation
python3 python/kobert_spark_testing.py
```

## Logging

### Training Logs
During fine-tuning, training progress is logged to both console and files:
- **Console Output**: Real-time training metrics (loss, learning rate, validation metrics)
- **Log Files**: Saved in `logs/{model}_{language}/` directory with timestamps
- **Format**: `logs/roberta_spark_english/20241208_143022.log`

### Evaluation Logs
Evaluation results are saved to timestamped log files:
- **Metrics**: Accuracy, F1, precision, recall for all classes and filtered metrics

### Log File Locations
```
logs/
├── roberta_spark_english/
│   └── evaluation_20241208_143022.log
├── bert_spark_english/
├── deberta_spark_english/
├── kobert_spark_korean/
└── krelectra_spark_korean/
```

## Configuration
### BERT (English)
- Model: `bert-base-uncased`
- Language: English
- Batch size: 16
- Learning rate: 2e-5
- Epochs: 5
- Max length: 256

### RoBERTa (English)
- Model: `SamLowe/roberta-base-go_emotions`
- Language: English
- Batch size: 16
- Learning rate: 1e-5
- Epochs: 10
- Max length: 256

### DeBERTa (English)
- Model: `microsoft/deberta-base`
- Language: English
- Batch size: 16
- Learning rate: 2e-5
- Epochs: 10
- Max length: 128
- Note: `fp16=False` and `adam_epsilon=1e-6` for numerical stability

### KoBERT (Korean)
- Model: `monologg/kobert`
- Language: Korean
- Batch size: 8
- Learning rate: 1e-5
- Epochs: 10
- Max length: 256

### KR-ELECTRA (Korean)
- Model: `snunlp/KR-ELECTRA-discriminator`
- Language: Korean
- Batch size: 8
- Learning rate: 1e-5
- Epochs: 10
- Max length: 256
## Output Files

After training, each model directory contains:
- `pytorch_model.bin` or adapter files
- `tokenizer.json`, `tokenizer_config.json`
- `label_mappings.pkl`: Label to ID mappings
- `test_predictions.csv`: Detailed predictions on test set

## Metrics

Models are evaluated on:
- Accuracy
- Macro Precision/Recall/F1
- Equal weighting across all emotion categories regardless of frequency

## Requirements

```bash
pip install transformers datasets torch peft accelerate bitsandbytes scikit-learn pandas
```

## Notes

- The dataset has 26 emotion classes, some with very few samples
- All models use single-label classification (CrossEntropyLoss)
- Rare emotion categories (≤9 training instances) are assigned to training set only
- Training time: 5~10 minutes each for BERT/RoBERTa/DeBERTa/KoBERT/KR-ELECTRA (using an NVIDIA GPU A100)
- Models are saved with the best checkpoint based on validation macro F1
